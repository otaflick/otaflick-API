<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Anime List</title>
</head>

<body>
    <h1>List of Anime</h1>

    <form class="search-container" action="/fetch-anime" method="post">
        <input style="margin-right: 5px; padding: 8px; width: 200px;" type="text" id="searchTerm" name="searchTerm"
            placeholder="Anime Name" required>
        <button class="submit-btn" type="submit">Fetch Details</button>
    </form>

    {{#if animeList.length}}
        {{#each animeList}}
        <div class="animeDetailsContainer">
            <div class="posterContainer">
                <img src="https://image.tmdb.org/t/p/original/{{this.poster_path}}" alt="Anime Poster" >
            </div>

            <div class="detailsTextContainer">
                <a href="/addAnime/{{this.id}}">
                    <h2>{{this.name}}</h2>
                </a>
                <p style="font-size: 0.8em; margin-bottom:5px">{{this.overview}}</p>
                <p style="font-size: 0.8em; font-weight: bold;">First Aired: {{this.first_air_date}}</p>
                <p style="font-size: 0.8em; color: #666;">Rating: {{this.vote_average}}/10</p>
                
                <div class="anime-badges">
                    {{#if this.genre_ids}}
                        {{#if (includes this.genre_ids 16)}}
                            <span class="badge animation">Animation</span>
                        {{/if}}
                        {{#if (includes this.genre_ids 10765)}}
                            <span class="badge sci-fi">Sci-Fi & Fantasy</span>
                        {{/if}}
                        {{#if (includes this.genre_ids 10759)}}
                            <span class="badge action">Action & Adventure</span>
                        {{/if}}
                    {{/if}}
                </div>
            </div>
        </div>
        {{/each}}
    {{else}}
        <div class="no-results">
            <h3>No anime found</h3>
            <p>Try searching with different keywords</p>
        </div>
    {{/if}}

</body>

<style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600&display=swap');

    body {
        font-family: 'Poppins', sans-serif;
        background: linear-gradient(135deg,  0%, #764ba2 100%);
        min-height: 100vh;
        margin: 0;
        padding: 20px;
    }

    h1 {
        text-align: center;
        color: white;
        font-weight: 600;
        margin-bottom: 30px;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    .search-container {
        display: flex;
        justify-content: center;
        margin-bottom: 30px;
        gap: 10px;
    }

    #searchTerm {
        padding: 12px 16px;
        width: 300px;
        border: none;
        border-radius: 25px;
        font-size: 16px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.1);
        transition: all 0.3s ease;
    }

    #searchTerm:focus {
        outline: none;
        box-shadow: 0 4px 20px rgba(0,0,0,0.2);
        transform: translateY(-2px);
    }

    .submit-btn {
        padding: 12px 24px;
        background: linear-gradient(45deg, #FF6B6B, #FF8E53);
        color: white;
        border: none;
        border-radius: 25px;
        cursor: pointer;
        font-size: 16px;
        font-weight: 500;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(255,107,107,0.3);
    }

    .submit-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 20px rgba(255,107,107,0.4);
        background: linear-gradient(45deg, #FF8E53, #FF6B6B);
    }

    .animeDetailsContainer {
        background: white;
        padding: 20px;
        display: flex;
        align-items: flex-start;
        width: 70%;
        margin: 20px auto;
        border-radius: 15px;
        box-shadow: 0 8px 25px rgba(0,0,0,0.1);
        transition: all 0.3s ease;
        border-left: 5px solid #FF6B6B;
    }

    .animeDetailsContainer:hover {
        transform: translateY(-5px);
        box-shadow: 0 12px 35px rgba(0,0,0,0.15);
    }

    .posterContainer {
        margin-right: 25px;
        flex-shrink: 0;
    }

    .posterContainer img {
        width: 120px;
        height: 180px;
        object-fit: cover;
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        transition: transform 0.3s ease;
    }

    .posterContainer img:hover {
        transform: scale(1.05);
    }

    .detailsTextContainer {
        text-align: left;
        flex: 1;
    }

    .detailsTextContainer h2 {
        margin-bottom: 10px;
        color: #333;
        font-weight: 600;
        font-size: 1.4em;
    }

    .detailsTextContainer p {
        margin: 5px 0;
        color: #666;
        line-height: 1.5;
    }

    a {
        text-decoration: none;
        color: inherit;
    }

    .detailsTextContainer h2:hover {
        color: #FF6B6B;
    }

    .anime-badges {
        margin-top: 10px;
        display: flex;
        gap: 8px;
        flex-wrap: wrap;
    }

    .badge {
        padding: 4px 12px;
        border-radius: 15px;
        font-size: 0.7em;
        font-weight: 500;
        color: white;
    }

    .badge.animation {
        background: linear-gradient(45deg, #4ECDC4, #44A08D);
    }

    .badge.sci-fi {
        background: linear-gradient(45deg, #FF6B6B, #FF8E53);
    }

    .badge.action {
        background: linear-gradient(45deg, #45B7D1, #96C93D);
    }

    .badge.fantasy {
        background: linear-gradient(45deg, #A166AB, #5073B8);
    }

    .no-results {
        text-align: center;
        color: white;
        margin-top: 50px;
    }

    .no-results h3 {
        font-size: 1.5em;
        margin-bottom: 10px;
    }

    /* Responsive design */
    @media (max-width: 768px) {
        .animeDetailsContainer {
            width: 90%;
            flex-direction: column;
            text-align: center;
        }

        .posterContainer {
            margin-right: 0;
            margin-bottom: 15px;
        }

        .search-container {
            flex-direction: column;
            align-items: center;
        }

        #searchTerm {
            width: 80%;
            margin-bottom: 10px;
        }
    }
</style>

{{!-- <!-- Handlebars helper for checking array inclusion -->
<script>
    // This would typically be registered in your server-side Handlebars configuration
    // For client-side demonstration, we're including it here
    Handlebars.registerHelper('includes', function(array, value) {
        return array && array.includes(value);
    });
</script> --}}

</html>